# 如何愉快的配置内网服务器代理上网

## 概述

实验室的内网服务器通常无法直接访问外网（GitHub、HuggingFace、pip 源、Google 等）。本文介绍如何让服务器借助你本地或某台机器上的**代理软件**（Clash、V2Ray 等）访问外网，核心就是设置 `http_proxy` / `https_proxy` 环境变量。

## 原理

程序读取 `http_proxy` / `https_proxy` 环境变量后，会把网络请求先发给代理服务器，再由代理转发到外网。所以你只需要告诉服务器「代理在哪个地址、哪个端口」即可。

## 前提条件

- 一个可用的代理服务器地址和端口（在 Clash 等软件的设置里查看，通常是 `7890` 之类的端口）。
- 服务器能访问到这个代理地址（同一内网，或代理机开了相应监听）。
- 基本的 Linux 终端操作能力。

> 提示：如果代理软件跑在你自己的电脑上，要确保它允许「局域网连接」（Clash 里叫 Allow LAN），并用电脑的内网 IP 作为 `proxy_server`，而不是 `127.0.0.1`。

## 临时配置（仅当前终端有效）

适合临时跑一下，关掉终端就失效，不污染环境：

```bash
export http_proxy=http://proxy_server:port
export https_proxy=http://proxy_server:port
# 有些工具认大写变量，建议一并设置
export HTTP_PROXY=$http_proxy
export HTTPS_PROXY=$https_proxy
```

把 `proxy_server:port` 换成你的实际地址，例如 `http://192.168.1.10:7890`。

用完想关掉代理：

```bash
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY
```

## 永久配置（每次登录自动生效）

写进 shell 配置文件，对当前用户长期有效：

```bash
vim ~/.bashrc        # bash 用户
# 或 vim ~/.zshrc    # zsh 用户
```

在文件末尾添加：

```bash
# 代理配置
export http_proxy=http://proxy_server:port
export https_proxy=http://proxy_server:port
export HTTP_PROXY=$http_proxy
export HTTPS_PROXY=$https_proxy
```

让配置立即生效（否则要重新登录）：

```bash
source ~/.bashrc
```

## 排除内网地址（重要）

如果给所有流量都走代理，访问内网其他机器（比如本地 pip 源、内网数据库）反而会失败。用 `no_proxy` 把内网地址排除掉：

```bash
export no_proxy="localhost,127.0.0.1,192.168.0.0/16,10.0.0.0/8"
export NO_PROXY=$no_proxy
```

## 特定应用的代理配置

环境变量对大多数命令行工具生效，但有些工具有自己的代理设置：

```bash
# Git
git config --global http.proxy http://proxy_server:port
git config --global https.proxy http://proxy_server:port
# 取消 Git 代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# pip（单次）
pip install -i https://pypi.org/simple some-package --proxy http://proxy_server:port

# conda
conda config --set proxy_servers.http http://proxy_server:port
conda config --set proxy_servers.https http://proxy_server:port
```

## 验证代理是否生效

```bash
# 看到 HTTP/200 或正常响应头，说明能通外网
curl -I https://www.google.com

# 直接看出口 IP，确认走的是代理
curl https://api.ipify.org
```

## 常见问题排查

- **`curl: (7) Failed to connect`：** 代理地址或端口错了，或代理软件没开「允许局域网连接」。
- **能 ping 通代理机但代理不工作：** 检查代理软件的监听端口是否对外开放，防火墙是否放行。
- **访问内网服务变慢或失败：** 没设 `no_proxy`，内网流量被错误地导向了代理。
- **`export` 只在当前终端生效：** 新开的终端 / tmux 窗口需要重新 source 或重新 export。

